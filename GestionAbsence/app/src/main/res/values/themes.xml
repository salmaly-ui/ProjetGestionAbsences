<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Thème de base Material Components (clair, sans ActionBar) -->
        <style name="Theme.GestionAbsence" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Couleurs personnalisées (optionnelles, utilise celles de ton colors.xml) -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>
    <style name="SplashTheme" parent="Theme.GestionAbsence">
        <item name="android:windowBackground">@drawable/bg_splash</item>
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">@color/primary</item>
    </style>
</resources>